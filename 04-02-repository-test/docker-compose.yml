# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: petscare
      POSTGRES_PASSWORD: petscare
      POSTGRES_DB: petscare
    ports:
      - "5432:5432"
